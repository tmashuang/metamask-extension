import assert from 'assert'
import { unconfirmedTransactionsCountSelector } from '../confirm-transaction'

describe('unconfirmedTransactionsCountSelector', () => {

  const state = {
    metamask: {
      unapprovedTxs: {},
      unapprovedMsgs: {},
      unapprovedPersonalMsgs: {},
      unapprovedTypedMessages: {},
      network: '',
    }
  }

  it('', () => {

  })
})